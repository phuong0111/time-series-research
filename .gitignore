# Heavy raw datasets (284G) — tracked out-of-band, not in git
dataset/*/
!dataset/_logs/

# Python
__pycache__/
*.py[cod]

# MOA benchmarks for data-drift/ (106 MB extracted) — refetch with data/fetch.sh
data-drift/data/*.arff
data-drift/data/*.zip
